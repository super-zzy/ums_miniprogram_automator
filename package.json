{
  "name": "miniprogram-automation-test",
  "version": "2.0.0",
  "description": "微信小程序自动化测试框架（重构版）",
  "scripts": {
    "test": "mocha tests/*.test.js --timeout 60000",
    "test:report": "mocha tests/**/*.test.js --reporter mochawesome --reporter-options reportDir=reports",
    "lint": "eslint ."
  },
  "dependencies": {
    "miniprogram-automator": "^0.10.0",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "chai": "^4.3.7",
    "chalk": "^4.1.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "eslint": "^8.45.0"
  }
}
